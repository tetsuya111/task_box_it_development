# hearing-chat-site の design.md に実装時の変更点を追記し、tasks.md を更新

## 何を行ったか

- `design.md` に「実装時の変更点」の節を追加し、未解決事項に「AnthropicLLMClient は実際のAPIでは未実行」を記載した。
- `tasks.md` の完了条件にチェックを入れ、ステータスを In Progress にした。未完了は、実際のLLMでの通し確認、トークン数の集計、ブラウザでの目視確認（スマートフォンの画面幅を含む）。

## なぜ行ったか

設計と実装のずれを後から追えるようにするため。主な変更は、見積もりと「現状の要約のDL」を補完つきの要件サマリーで作成するようにした点（補完なしだと、6項目がそろうまで対話の途中で試せないため）。
