# 各項目の説明は docs/frontend.md を参照。コピーして .env.local を作成する

# バックエンドのオリジン（backend の CORS_ALLOWED_ORIGINS と対になる）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Google Cloud Console で発行した OAuth クライアントID（backend の GOOGLE_OAUTH_CLIENT_ID と同じ値）
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
